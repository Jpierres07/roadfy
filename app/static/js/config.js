// Configuración de la aplicación
const CONFIG = {
    // Usar rutas relativas ya que el frontend está servido desde el mismo servidor
    API_BASE_URL: '/api',
    FRONTEND_URL: window.location.origin,
    STORAGE_KEYS: {
        TOKEN: 'access_token',
        USER: 'user'
    }
};

